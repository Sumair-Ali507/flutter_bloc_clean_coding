// In android/build.gradle (Groovy)

allprojects {
    repositories {
        google()
        mavenCentral()
    }

    buildDir = "../../build/${project.name}"
}

tasks.register("clean", Delete) {
    delete rootProject.layout.buildDirectory
}
